{
  "private": true,
  "name": "mobx-state-tree-next-root",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build --stream",
    "test": "lerna run test --stream"
  },
  "workspaces": [
    "packages/mobx-state-tree-next"
  ],
  "devDependencies": {
    "lerna": "^3.0.5"
  }
}
