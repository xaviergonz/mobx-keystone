{
  "private": true,
  "name": "root",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "lib:build": "lerna run build --stream --scope lib",
    "lib:build-docs": "lerna run build --stream --scope lib",
    "lib:test": "lerna run test --stream --scope lib",
    "site:start": "lerna run start --stream --scope site",
    "site:build": "yarn lerna run start --stream --scope site",
    "build-netlify": "yarn lib:build && yarn lib:build-docs && yarn site:bild"
  },
  "workspaces": [
    "packages/lib",
    "packages/site"
  ],
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
