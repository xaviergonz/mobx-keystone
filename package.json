{
  "private": true,
  "name": "root",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "start-site": "lerna run start --stream --scope site"
  },
  "workspaces": [
    "packages/lib",
    "packages/site"
  ],
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
